/* eslint-disable @typescript-eslint/no-unused-vars */
interface User {
    user_id: string;
    email: string;
    username: string;
    message: string;
  }
  
  interface Token {
    id?: string;
    email?: string;
  }